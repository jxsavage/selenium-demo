{
  "name": "selenium-standalone",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "mocha src/*.js --inspect --reporter spec",
    "test_cart": "mocha src/*.js --inspect --reporter spec 'shopping cart page functionality'",
    "start": "node src/index.js",
    "find": "sudo ps aux | grep -i chromedriver"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "chai": "^4.3.6",
    "chromedriver": "^101.0.0",
    "mocha": "^10.0.0",
    "selenium-webdriver": "^4.1.2"
  }
}
